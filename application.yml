# 一定要保存为UTF-8格式

spring:
  profiles:
    active:
      - dev

---
spring:
  # 开发环境
  profiles: dev
  application:
    name: microservicecloud-config-maory-dev

---
spring:
  # 测试环境
  profiles: test
  application:
    name: microservicecloud-config-maory-test